<% title(@project.name, "Project") %>
<h1 class="header">
  <%= @project.name %>
  <div class="links">
    <%= link_to "Edit", edit_project_path(@project), class: "edit" %>
    <%= link_to "Delete", project_path(@project), method: :delete, class: "delete" %>
  </div>
</h1>
<p><%= @project.description %><p>

<h2 class="header">
  Tickets <%= link_to "New Ticket", new_project_ticket_path(@project), class: "new" %>
</h2>
