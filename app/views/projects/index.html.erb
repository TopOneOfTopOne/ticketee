<h1>The index baby</h1>
<%= link_to "New Project", new_project_path %>
<% @projects.each do |project| %>
  <ul>
    <h2><%= link_to project.name, project %></h2>
    <p>
      <%= project.description %>
    </p>
  </ul>
<% end %>
